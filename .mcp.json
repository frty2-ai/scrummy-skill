{
  "mcpServers": {
    "scrummy": {
      "type": "http",
      "url": "${SCRUMMY_URL}/mcp/?workspace=${SCRUMMY_WORKSPACE:-}",
      "headers": {
        "Authorization": "Bearer ${SCRUMMY_TOKEN}"
      }
    }
  }
}
