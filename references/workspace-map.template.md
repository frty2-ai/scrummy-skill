# Workspace map — `<slug>`

A cache, not gospel: live calls win when they disagree, and new projects will not be here. Rebuild with `list_projects` and `list_workspace_members` when something looks missing.

Cached: <date>

## Principal

**<name>** — <email>, id `<uuid>`. Role: <admin/member>. Notes on how they work: <e.g. starts late and usually gets away with it; parks everything on themself; prefers Slack over comments>.

## Roster

| Name | id | Role | Usually in |
|---|---|---|---|
| | | | |

Names that appear in conversation but are not workspace members: <list>. Do not invent assignees for them; note the intended owner in a comment or offer to invite.

## Projects

| Project | Identifier | id | Timezone | What it is for |
|---|---|---|---|---|
| | | | | |

## Routing hints

- <"anything about leads or proposals → BD">
- <"MSC work splits two ways: … ask which; misrouting here is expensive">

## Structure

- Which projects have cycles, and the cadence (e.g. two-week, starts Monday).
- Which projects have modules and what a module means there (campaign, client phase, epic).
- Non-default states worth knowing (e.g. "Ready for review" is in the `started` group).
- Sequencing or domain labels the team actually uses.
- Where decision records live (a "Decisions" page per project, or comments).
